# BuyerLab AI

AI-powered virtual buyer simulation platform for pre-launch e-commerce testing.

Initial stack: Streamlit, LangGraph, and Gemini API.

Run locally:

```bash
pip install -r requirements.txt
streamlit run app.py
```
